---
- name: Converge
  hosts: all
  gather_facts: false
  vars:
    ansible_user: root
    host: ""  # Docker container can't set hostname
    timezone: ""  # Docker container can't set timezone
  roles:
    - role: fred_drake.debian_setup
